# Candidate generation script reads comma-separated keys from the environment.
# Copy locally to .env only if your launcher explicitly loads it; never commit values.
GLM_API_KEYS=
DEEPSEEK_API_KEYS=
MEDICAL_VALIDATION_DIR=
